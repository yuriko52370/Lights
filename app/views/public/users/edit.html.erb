<div class="container">
  <h1>Edit</h1>
  <p>ユーザーネームの変更</p>

  <%= form_with model: @user do |f| %>
  <h4>User Name</h4>
  <%= f.text_field :name %>

  <%= f.submit '更新' %>
  <% end %>
  <%= link_to'Back', mypage_user_path, class: 'btn btn-info' %>
</div>